# ============================================================================
# REACT APP PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# Create a file named .env.production in the client folder
# These variables will be used when building for production

# Backend API URL
# Replace with your actual backend URL (Railway, Render, VPS, etc.)
REACT_APP_API_URL=https://api.yourdomain.com

# Stripe Publishable Key (Production)
# Get from: https://dashboard.stripe.com/apikeys
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_live_your_production_stripe_publishable_key

# App Configuration
REACT_APP_NAME=DocConnect
REACT_APP_VERSION=1.0.0

# Feature Flags (Optional)
REACT_APP_ENABLE_ANALYTICS=true
REACT_APP_ENABLE_CHAT=true

# External Services (Optional)
# Google Analytics
# REACT_APP_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry (Error Tracking)
# REACT_APP_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
# 1. All React env variables MUST start with REACT_APP_
# 2. These are embedded in the build and are PUBLIC
# 3. Never put sensitive secrets here (API keys, passwords, etc.)
# 4. Only put the Stripe PUBLISHABLE key (pk_live_...), never the secret key
# 5. After changing these, you must rebuild: npm run build
